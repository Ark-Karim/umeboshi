{
  "$schema": "./registry.schema.json",
  "description": "Umeboshi plugin registry — maps backend types to active plugins. To switch backends, change the plugin name here. Plugins are auto-discovered from subdirectories matching their type.",
  "backends": {
    "transcribe": {
      "active": "local-whisper",
      "description": "Converts audio/video files to text. Default: local Whisper (English, no API key). Add plugins/transcribe/<name>/SKILL.md for external services."
    },
    "generate": {
      "active": "claude-code",
      "description": "Generates Anki cards from structured text. Default: Claude Code + DeepSeek."
    },
    "import": {
      "active": "ankiconnect",
      "description": "Imports generated cards into Anki. Default: AnkiConnect (localhost:8765)."
    },
    "notetype": {
      "active": "umeboshi-kaname",
      "description": "Card rendering template (Front/Back/Styling). Default: Umeboshi-Kaname."
    },
    "sync": {
      "active": "none",
      "description": "Optional cloud sync for LLM Q&A across devices. Set to \"claudeflare\" to enable. Default: disabled (all data stays local)."
    }
  }
}
