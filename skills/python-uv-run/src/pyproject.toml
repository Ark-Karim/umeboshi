[project]
name = "python-scripts"
version = "0.1.0"
description = "Temporary Python scripts"
requires-python = ">=3.10"
dependencies = [
    "pymupdf>=1.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["temp"]
